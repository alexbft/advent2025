pub fn solve(input: &str) -> usize {
    0
}

#[cfg(test)]
mod tests {
    use super::*;
    use indoc::indoc;

    #[test]
    fn test_solve() {
        let input = indoc! {"
        "};
        assert_eq!(solve(input), 0);
    }
}